# TENANCY AUDIT — Badminton Queue App (prod `usxftpexoimletqmrggb`)

## 1. VERDICT

> **✅ CLOSING BANNER — 2026-08-13: every finding in this document is dispositioned.**
>
> ⚠️ **Read that literally: it is a claim about the findings *listed here*, not about the defect *classes* they belong to.** The distinction is not pedantic — it was violated the same day this banner was written. Re-running #4/#10's "authorize on A, operate on B" pattern by hand across all 40 `isSessionOrganizer(` call sites turned up **two live instances neither sweep had covered**, now recorded as **#12** and fixed in code. A finding list is closed when every row has a disposition; a *class* is closed only when someone enumerates the call sites and checks each one. Do not read the row count below as coverage.
>
> ⚠️ **And that enumeration was itself not the whole class.** It asks "which id does the write use?", which is blind to *guard order* — so all seven `sessions.ts` call sites passed it while `closeSession` was leaking a session-UUID existence-and-status oracle, recorded as **#12(c)**. The sweep could not have found it. **An audit of one shape closes that shape, not the file.**
>
> ⚠️ **And #12(c) was not the whole of *that* class either — sweeping guard-ORDER on its own turned up five more, recorded as #12(d).** `endMatchInternal`, `updateMatchDetails`, `cancelMatchAction` (`match-lifecycle.ts`), `clearOnDeckMatch` (`match-drafts.ts`) and `swapPlayerInMatch` (`swap-player.ts`) each answered a distinct "not found" — one of them carrying the raw PostgREST error text — to any authenticated caller, for any match UUID, through the **service client**, before any authorization ran. Two of the three levels of this audit's own closure claim have now been reopened by the next sweep down. **Each "class closed" claim is only as wide as the question the sweep asked.**
>
> 🪤 **#12(c) was not found on that branch at all — the review gate found it two days earlier and it was filed, not fixed.** `git log -S` puts the sentence *"`closeSession` returns `alreadyClosed` **before** the organizer check (pre-existing; leaks one bit)"* in commit `6592864` (**2026-08-11**, PR #55), under a `MEMORY.md` heading literally named **"Booked minors (review-agent 'Minor issues' pass, per CLAUDE.md gate rule 3)"**. The gate ran and reported it; nothing owned the follow-through. **A booked minor with no owner is not a disposition** — the same failure mode this document's §1 closing banner records for the ten headings that kept their as-found severity long after their fixes shipped.
>
> **The verdict paragraphs below are preserved as written on 2026-07-21 (and 2026-07-23) — apart from an inline `*(✅ CLOSED …)*` stamp this pass inserted after the bold lead-in of **two paragraphs in §1** — the 2026-07-23 update and the PIN-chain lead (the latter is not itself a dated verdict paragraph) — which is the only edit made to any of them; the 2026-07-21 verdict paragraph itself is untouched and is disclaimed by this banner alone — and are now HISTORICAL. Do not read them as a current statement of risk.** Neither should the severity labels on the `### #n` headings in §2 — those describe each finding *as found*, and **ten stale headings** still carried their as-found severity, with no disposition, long after the finding was closed — weeks for most of them, three days for #11's duplicate, whose fix was applied 2026-08-10. Five of the ten read the literal `EXPLOITABLE TODAY` (#1, #2, #3, #6 and #11's duplicate); the rest are variants — `🔴 CRITICAL · UNAUTHENTICATED WRITE TODAY` (#10), `🟠 HIGH · club boundary void TODAY, cross-club at 2nd club` (#5), `🟡 MEDIUM · cross-session TODAY` (#4), `🟡 MEDIUM · TODAY` (#7) — and #8's `🟡 MEDIUM · mostly latent until 2nd club` names no date at all but still read as unresolved. For **nine findings** (#1–#8, #10) that was the only heading they had; #11 also had one, on its older duplicate entry, while its authoritative copy was already stamped. Only #9's `⚪ LOW · negligible` read harmlessly. That gap is itself the defect this banner exists to remove: on 2026-08-13 a later analysis treated #2's un-updated heading as evidence its hole was still open, and was wrong. **A finding with no status box is not evidence that the hole is open — read the code.** Every finding now carries a disposition, either a status box or — for #9 — an explicit ⚖️ ACCEPTED stamp on the heading. The rot was not confined to headings: **four** further paragraphs still described live exposures or live rankings in the present tense — two inside long-closed #6 (the `refresh_alltime_leaderboard` "DoS lever" and the "second-most-impactful … live today" ranking), one in §3 "WHAT IS SOLID" (`v_alltime_leaderboard_mat`), one in §4's urgency framing — plus the two dated verdict paragraphs this banner covers — the 2026-07-21 verdict, disclaimed by this banner alone, and the 2026-07-23 update, which carries an inline stamp. §1's third paragraph, the PIN-chain lead, is stamped in place as well. All are corrected in place below rather than deleted.
>
> Re-verified 2026-08-13 against the working tree and against prod `usxftpexoimletqmrggb` (read-only), not inferred from the PR log:
>
> | # | Sev. as found | Disposition | Evidence re-checked 2026-08-13 |
> |---|---|---|---|
> | #1 | 🔴 CRITICAL | ✅ FIXED | all four `profile.ts` actions call `isPlayerInSessionScope(userId, sessionId)` after the organizer gate (`:48/:89/:111/:148`) |
> | #2 | 🟠 HIGH | ✅ FIXED | `sessions.ts:140-146` requires `clubId` + `isClubAdmin`; `clubs.ts:161` filters `is_active` |
> | #3 | 🟠 HIGH | ✅ FIXED · clause 3 declined | fail-closed `cojoin_record_and_check` RPC gate; passcode 100 → 100 000 values. The club-scoping clause is **declined** — see §2 #3 |
> | #4 | 🟡 MEDIUM | ✅ FIXED | `match-events.ts:50` — `.eq("session_id_snapshot", sessionId)` |
> | #5 | 🟠 HIGH | ✅ FIXED | both shims gate `ensureClubMembership` on a participation signal (`play:74`, `organizer:135`) |
> | #6 | 🟠 HIGH | ✅ FIXED · 1 accepted residual | prod: all three RPCs + the matview are `false` for anon **and** authenticated. Residual = the share-link contract, §2 #6 |
> | #7 | 🟡 MEDIUM | ✅ FIXED · clause 3 declined | private topic + `realtime.messages` policy; names stay, see §2 #7 |
> | #8 | 🟡 MEDIUM | ✅ FIXED | `profiles_select` = `id = auth.uid() OR can_read_profile(id)`, five arms |
> | #9 | ⚪ LOW | ⚖️ ACCEPTED — no fix possible | realtime bypasses RLS on DELETE by design; re-confirmed 2026-08-10 |
> | #10 | 🔴 CRITICAL | ✅ FIXED | prod: **zero volatile _non-trigger_** SECDEF functions hold anon EXECUTE — all 16 are `false` for anon. The 9 SECDEF functions that still are: 6 `STABLE` RLS helpers (policies invoke them as the calling role — 4 are actually referenced by one, `is_match_club_member`/`is_session_club_member` by none) + `lookup_active_session`, the `STABLE` anon join-flow lookup, which is not a predicate at all (`RETURNS TABLE`) + `handle_new_user`/`handle_new_session`, which are volatile but `RETURNS trigger`, so PostgREST cannot call them |
> | #11 | 🟡 MED-LOW | ✅ FIXED | migration applied to prod, stamp `20260810151355`, truth table verified |
> | #12 | 🟡 MEDIUM ×2 + 🔵 LOW | ✅ **CLOSED — SHIPPED + DEPLOYED 2026-08-13** (PR #64 → `main` `8f4cb78`, Vercel Production `success`) | found 2026-08-13, *after* this table was first written. **(a)** `swapMatchPlayers` guard 3b (+ guard 2 moved above guard 3); **(b)** **two** court gates on the `callNextMatch` path — one in the action, one at the top of `promoteOnDeckMatchInternal`; **(c)** `closeSession`'s organizer gate moved above the session fetch, closing a session-UUID existence-and-status oracle — **booked by the review gate on 2026-08-11 (`6592864`, PR #55) as a "minor" and left unowned for two days**, not newly discovered; **(d)** the same ordering defect at **five** more sites, found by then sweeping guard-ORDER as its own class: `endMatchInternal`, `updateMatchDetails`, `cancelMatchAction`, `clearOnDeckMatch`, `swapPlayerInMatch`. Each of the **ten** regression pins — two binding (`M-14`, `Test 8b`) and **eight** ordering (`M-14b`, `M-14c`, `Test 1b`, `PA-1`…`PA-5`) — proven by removing/reordering the guard and watching the cross-tenant write, or the oracle, actually come back. TypeScript-only — no migration. See §2 #12 |
>
> Prod migration stamps confirmed present: `20260722165217`, `20260722165729`, `20260724050127`, `20260724050234`, `20260724050312`, `20260724050436`, `20260724054744`, `20260810151355`. **This audit spawned three non-code items, which are NOT findings. Exactly one remains open:** the optional project-wide "Allow public access" dashboard toggle (`MEMORY.md` → STANDING TO-DO item **E**). The other two are closed. (a) A live #7 broadcast-delivery smoke test, carried as a user to-do from 2026-07-24, **discharged 2026-08-12** (item **C**): `tests/e2e/scenario-r-resilience.spec.ts` already drives two real organizer contexts against production, and `[R-1] [R-2] [R-5]` went 4/4 green there. (b) Enabling leaked-password protection, **closed WON'T DO 2026-08-04** — the toggle is Pro-plan-gated and this org is on Free, so it is not actionable, and there is nothing real for it to protect (`MEMORY.md` → STANDING TO-DO, historical 2026-07-29 list, item 2). ⚠️ One word of the recorded rationale is wrong, corrected 2026-08-13: it says *no* email+password credential exists for HIBP to check, and **exactly one does**. Measured on prod: `auth.identities` holds 1 `email` identity beside 21 `google`, and it is the Playwright E2E organizer bot (`…@playwright.local`, a 60-char `$2a$` bcrypt). It is the only row in `auth.users` with a real hash: of the other 225, **35 hold NULL and 190 hold the empty string** — placeholders, not hashes (184 of the 190 are anonymous walk-in rows). Walk-ins hold no password at all — they sign in anonymously; the only secret they choose is the 4-digit **reconnect** PIN on `profiles`, which is not an auth credential and which HIBP never sees. So the conclusion holds: no human user of this app has a password, and the one that exists is a test fixture. *(An earlier draft of this box stamped the whole rationale false, on a count of `encrypted_password IS NOT NULL` — which counts those empty-string placeholders as credentials — and then asserted walk-in passwords were machine-generated, when walk-ins have no password at all. Both were wrong. `IS NOT NULL` is not a has-a-credential test; use `coalesce(encrypted_password,'') <> ''`.)* *(The remaining open items in that STANDING TO-DO — **A**, the cross-court live-session smoke test, and **D2**, a scheduled prod table DROP — belong to unrelated work, not to this audit.)*

**No. Club/session segregation is not sound today — a logged-in user (including a throwaway anonymous account) can reach data and even take over accounts they were never granted.** The DB's RLS is well-built and its helper functions are correct, but the application layer routinely bypasses RLS with the service-role client and then authorizes on the *wrong identifier*, so the RLS boundary is defeated from above. The environment currently has exactly **one club (CHILLAX, 183 active members, 23 sessions)**, which masks every *cross-club* finding — but several holes are exploitable **right now, within that one club**, and the rest go live the instant a second club onboards.

> **Update 2026-07-23 — the headline changed.** *(✅ CLOSED 2026-07-24 — historical, like the paragraph above it. #10 was fixed the next day; re-measured on prod 2026-08-13, all 16 are `false` for anon. Present tense below = as of 2026-07-23.)* A finding that surfaced *after* this verdict was written (**#10**) is worse than everything below it: **16 mutating `SECURITY DEFINER` RPCs hold `EXECUTE` for `anon`**, so match creation, live-roster rewrites, draft deletion, audit-event forgery and stats recomputation are reachable over PostgREST with the public anon key and **no login at all** — verified by curl against prod. Everything else in this audit requires an account. Fix #10 first; the rest of this ordering is otherwise unchanged.

**The single most important *authenticated* hole:** *(✅ CLOSED — see §2 #1 and #2. Historical, as written 2026-07-21.)* the **PIN account-takeover chain** in `src/app/actions/profile.ts`. Those four organizer actions verify that the caller organizes *some* session (the `sessionId` argument) but never verify that the *target* `userId` has anything to do with that session or its club. Combined with the ability for any authenticated user to self-provision an organizer session (`createSession` with no `clubId`), this lets anyone read or overwrite the PIN of any of the 183 real members — and since a name+PIN drives `reconnectPlayer`'s identity migration, that is full account takeover. Fix the authorize-on-A/operate-on-B decoupling first; everything else is read-only leakage by comparison.

---

## 2. CONFIRMED ISSUES (ranked by real-world risk)

### #1 — PIN read/reset with an unbound target → account takeover  ✅ CLOSED · fixed in code (was 🔴 CRITICAL)
**File:** `src/app/actions/profile.ts` (`getPlayerPin` :72, `resetPlayerPin` :90, `updatePlayerPin` :114, `updatePlayerSkill` :31)

- **What leaks / breaks:** Any member's 4-digit PIN can be read or overwritten, and their skill edited. The PIN is the reconnect credential; display names are readable by every authenticated user (`profiles_select USING(true)`). Verified: `reconnectPlayer(name, pin)` (auth.ts:224) finds the profile by name+PIN and migrates the identity onto it via `migrate_player_identity` — so PIN + known name = **full account takeover**.
- **Who can reach it:** Any authenticated user who organizes *any* session. All four actions gate only on `isSessionOrganizer(user.id, sessionId)` and then run a service-role read/write against the **global** `profiles` table keyed by the caller-supplied `userId`, which is never checked against the session or its club.
- **Concrete attack (verified mechanics):** `signInAnonymously` → `createSession({name:'x', scoring:'first_to_21'})` with **no** `clubId` (see #2) → you are now organizer of a real session → `getPlayerPin(mySessionId, VICTIM_ID)` returns e.g. `4821` → `reconnectPlayer(victimName, '4821')` migrates their account onto your new identity. `resetPlayerPin`/`updatePlayerPin` achieve takeover even without reading the PIN.
- **Today vs later:** Exploitable **today** against all 183 CHILLAX members — needs no second club. Any of the club's legitimate organizers can already do this to any member; #2 just removes the "must be an organizer" precondition entirely.
- **Fix:** After the organizer gate, prove the target `userId` shares scope with `sessionId` — require a `queue_entries` row for `(sessionId, userId)` **or** a `club_members` row for `(session.club_id, userId)` — before the service-role read/write. Reject otherwise. The id that authorizes and the id that is operated on must be proven to be in the same scope.

> **✅ STATUS — CLOSED (box added 2026-08-13, fix landed earlier).** All four actions now run the third gate: `updatePlayerSkill:48`, `getPlayerPin:89`, `resetPlayerPin:111`, `updatePlayerPin:148` each call `isPlayerInSessionScope(userId, sessionId)` and return before touching the service client. `_shared.ts:139-154` implements it as a `queue_entries` lookup on `(session_id, player_id)` — **narrower than the prescribed fix**, which also allowed a bare `club_members` row: a co-member who has never queued in this session is now rejected too. That is stricter, not weaker, and it is the right call — the organizer's authority is over their session's queue, not the whole club roster. Both the `sessionId` and `userId` parameters are load-bearing. ⚠️ The header comment at `profile.ts:8-19` enumerates the three gates correctly, with one stale clause — in the direction of the *prescribed* fix rather than the shipped one: `:11-12` says the target must belong to the session "(its queue) **or its club**", and the club arm was never implemented; `isPlayerInSessionScope` is queue-only. *(`:18`'s "so **both** gates can be verified" is **not** a third-gate miscount, as an earlier draft of this box claimed — it means the two gates that consume `sessionId`, and `607df4e` widened it from the singular "the organizer gate" at exactly the commit that added gate 3. Verified against `git show 607df4e^`.)* The gate *calls* are at `:45/:48`, `:86/:89`, `:108/:111`, `:145/:148`; read those, not the header. Application layer only, no migration.

### #2 — `createSession` self-provisions organizer rights with no membership  ✅ CLOSED · fixed in code (was 🟠 HIGH)
**File:** `src/app/actions/sessions.ts:113`

- **What breaks:** The club-admin check `if (!(await isClubAdmin(...)))` runs **only** when a non-empty `clubId` is passed. Omit `clubId` and the branch is skipped; the row inserts with `created_by = user.id` and the DB DEFAULT club (CHILLAX). `isSessionOrganizer` treats `created_by` as the fast-path organizer (confirmed `_shared.ts:94`), so a fresh anonymous user instantly holds the entire organizer server-action surface for a real session in the founding club without being a member of it.
- **Who / today:** Any authenticated user, **today**. This is the privilege-provisioning primitive that turns #1, #4, live swaps, and roster reads from "needs an organizer" into "needs any login."
- **Fix:** Require an explicit club context and always verify `isClubAdmin` (or at minimum `isClubMember`) for the resolved club. Treat a missing `clubId` as an error, not an implicit grant into CHILLAX.

> **✅ STATUS — CLOSED, exactly as prescribed.** `src/app/actions/sessions.ts:140-146`: `clubId` is now required (missing → `"A club is required to create a session."`), validated as a UUID, and gated on `isClubAdmin`, which resolves through `getClubRole`'s `is_active = true` filter (`src/lib/clubs.ts:161`). The DB-DEFAULT-club escape hatch is gone. Application layer only, no migration. **This box was added 2026-08-13** — the fix landed earlier, but the finding sat under a `🟠 HIGH · EXPLOITABLE TODAY` heading long after it was closed, and a later analysis wrongly treated that heading as evidence the hole was open. Residual to keep in mind, not a new finding: sessions created *before* the fix may still have a `created_by` who is not a club member.

### #3 — `joinAsCoOrganizer`: ~100-value passcode, no rate limit, cross-club search  ✅ CLOSED · 2 of 3 clauses shipped, the third declined (was 🟠 HIGH)
**File:** `src/app/actions/sessions.ts:222` (generator :48)

- **What breaks:** `generatePasscode()` = one of 10 `BIRDIE_WORDS` + one digit = **100 possible values (~6.6 bits)**, using `Math.random()`. `joinAsCoOrganizer` does a service-role lookup across **all** active sessions in **all** clubs (`.eq('is_active',true).eq('organizer_passcode',normalized)`) with no club scoping, no throttling, no lockout. A hit inserts a `session_organizers` row → full co-organizer rights (roster reads, PIN actions per #1, match events, live swaps).
- **Who / today:** Any authenticated user can walk all 100 passcodes in ~100 POSTs and co-organize whichever active sessions currently hold them. Live today; becomes cross-club at a second club.
- **Fix:** Add per-user/IP rate limiting and lockout; raise entropy (more chars, `crypto.getRandomValues`); scope the lookup to a known club context rather than searching every active session globally.

> **✅ STATUS — CLOSED (box added 2026-08-13, fix landed earlier).** Read the three clauses separately; only two shipped.
>
> **Clause 1 — rate limit + lockout: SHIPPED.** `cojoin_record_and_check(p_user_id, p_ip, p_window_min, p_user_max, p_ip_max)` records the attempt and returns the in-window verdict in **one transaction**, replacing a read-then-write pair that concurrent serverless invocations could all pass together. It is **fail-closed**: `gateErr || !gate` denies the join. Limits are 10 failures per account and **60 per IP** per 15 min — the IP budget is deliberately loose because a badminton club is the canonical single-NAT environment (one gym Wi-Fi, CGNAT mobiles), so a tight one is a griefing DoS; 60/15min is still ~17 days of brute force from one IP. Confirmed on prod: `cojoin_record_and_check` holds EXECUTE for `service_role` only, not `anon`.
>
> **Clause 2 — entropy: SHIPPED, both halves.** `generatePasscode()` (`sessions.ts:66-70`) is `BIRDIE_WORDS[randInt(BIRDIE_WORDS.length)]` + `String(randInt(10000)).padStart(4,"0")` (the array holds exactly 10 words) — **100 → 100 000 values**, a 1000× widening — and `randInt` (`:49-53`) is `crypto.getRandomValues` over a `Uint32Array`, so the `Math.random()` half of the remedy shipped too. The word list stays 10 values and is public; all the entropy is in the digits, which is what the doc comment at `:59` says. *(Pedantic residual, not a finding: `arr[0] % 10000` carries modulo bias over 2³², but the skew is ~2×10⁻⁶ relative and no attacker can act on it through a 10-failure lockout.)*
>
> **Clause 3 — scope the lookup to a club: DECLINED, and the decline is the design.** The join is still a global search across all active sessions. It has to be: the passcode is the *only* thing the joiner holds — that is what "co-organizer joins using ONLY the passcode" means (`sessions.ts:8`), and it is how a delegated organizer with no `club_members` row gets in at all. There is no club context to scope to at that moment without adding one to the flow, which would defeat it. What the global search would have leaked — whether a given passcode exists anywhere — is closed instead by the uniform `INVALID` message on every miss plus the lockout. **Re-open only if the passcode ever stops being the sole credential.**

### #4 — `getMatchEvents` authorizes on `sessionId` but reads by `matchId`  ✅ CLOSED 2026-07-23 (was 🟡 MEDIUM) — status box below #5
**File:** `src/app/actions/match-events.ts:24`

- **What leaks:** Gates on `isSessionOrganizer(user.id, sessionId)`, then queries `match_events` (service role) with only `.eq('match_id_snapshot', matchId)` — `matchId` is never bound to `sessionId`. An organizer of one session can pass any other session's `matchId` and receive that match's full audit trail: player movements (jsonb), actor names, payloads, phases.
- **Who / today:** Any self-provisioned organizer (#2) → cross-session **today** within CHILLAX; cross-club later.
- **Fix:** Fetch the match, confirm `match.session_id === sessionId` (mirror the guard already in the swap/fix actions) before returning; or add `session_id_snapshot = sessionId` to the query.

### #5 — Legacy shims `/play/[sessionId]` & `/organizer/[sessionId]` self-enroll any visitor into the session's club  ✅ CLOSED 2026-07-23 (was 🟠 HIGH)
**Files:** `src/app/play/[sessionId]/page.tsx:31`, `src/app/organizer/[sessionId]/page.tsx:29`; sink `ensureClubMembership` (`src/lib/clubs.ts:305`)

- **What breaks:** Both shims resolve the owning club from a **client-supplied session UUID** (`resolveSessionClubSlug` — service role, no auth, no `is_active` filter) and then call `ensureClubMembership(slug, user.id)` for **any** authenticated visitor. `ensureClubMembership` service-role-INSERTs an active `club_members role='member'` row with **no** invite/tie check — possession of the slug is treated as sufficient. The `(full)` layout's `requireClubMembership` then passes because the row was just written, granting member-level RLS read over the whole club: every session, the full roster (names + skill), all published/completed matches, queue, courts, club leaderboards, and a slot in the club switcher.
- **Reachability:** Session UUIDs are semi-public — printed into the intentionally shareable `/tv/[id]` (fully public), `/leaderboard/[id]`, and `/wrapped/[id]/[playerId]` links and QR codes. So the "secret" to join a club is a value the product deliberately distributes.
- **Today vs later:** With one club, this means **anyone can become a CHILLAX member on demand**, collapsing the only membership boundary that exists (roster, session list, club leaderboards). The *cross-club* headline ("read a club you don't belong to") is latent until a second club exists — but the mechanism is live now.
- **Fix:** Do not grant membership as a side effect of viewing a link. In both shims drop `ensureClubMembership` and just 308-redirect, letting `requireClubMembership` gate (non-members already bounce to `/play`). If walk-in convenience is needed, enroll only on a genuine tie (an existing `queue_entries`/`match_players` row in that session) or a token-validated `/c/[clubSlug]/join` invite — never on bare possession of a session UUID or a guessable slug.

> **STATUS 2026-07-23 (PR3 — `fix/tenancy-pr3-session-binding`): #4 and #5 both CLOSED.** Application layer only, no migration.
>
> **#4** — `.eq("session_id_snapshot", sessionId)` added to the `getMatchEvents` read, so the id that was authorized is the id that scopes the query. `session_id_snapshot` is `NOT NULL` and survives the match's `ON DELETE SET NULL`, and it is a residual on the existing `(match_id_snapshot, seq)` index. Prod integrity verified before shipping: 648 events / 465 matches, **0 null snapshots and 0 rows where the snapshot disagrees with the live match's `session_id`** — the filter excludes nothing legitimate.
>
> **#5** — the enroll survives, but only for the case it exists for, gated on a real participation signal: the organizer predicate (`created_by` OR `session_organizers` OR an active owner/admin of the session's club) for `/organizer/[id]`, an existing `queue_entries` row for `/play/[id]`. Both lookups go through the service client **because that is the authorization check itself** — `queue_entries` RLS is `session_access_level(session_id) IS NOT NULL` with no "own row" arm, so the caller's own client cannot see the row that proves a non-member walk-in belongs there. Both shims still 308 either way; the club route's `requireClubMembership` stays the single authority on what a non-participant may see.
>
> **The review caught a blocker worth recording as a standing rule.** The first draft imported `isSessionOrganizer` from `src/app/actions/_shared.ts` on the premise that a `"use server"` module's exports are public endpoints regardless. **That is backwards.** Such a module imported by another *action* module registers nothing; imported by anything in the **RSC layer** (a `page.tsx`), every export becomes a dispatchable Server Action scoped to that route. Measured by diffing `.next/server/server-reference-manifest.json`: **70 → 74 actions**, the +4 being `getAuthenticatedUser`, `getActorContext`, `isSessionOrganizer`, `isPlayerInSessionScope` under `app/organizer/[sessionId]/page` — two cross-tenant oracles over a caller-supplied uuid and an unauthenticated uuid → display-name lookup. The fix replaced it with a local, non-exported copy (back to 70, zero `_shared` entries), and `TB-IMPORT` in `tests/unit/tenancy-session-binding.test.ts` now fails if either shim imports anything under `app/actions/`. **The rule is "do not publish tenancy predicates", not "never import a `"use server"` module from RSC"** — the app does the latter deliberately for `getTvData` and `getWrappedData`, which are public reads on public routes and legitimately appear in the manifest. What must not be published is a `(userId, sessionId) → boolean` authorization oracle or a uuid → display-name lookup; when one of those needs sharing, it belongs in a `server-only` lib.
>
> **Tests:** `tests/unit/tenancy-session-binding.test.ts`, 19 tests (TB-EVENTS · TB-PLAY · TB-ORG · TB-IMPORT), mutation-checked eight-for-eight. The shim tests all assert on `ensureClubMembership` having been called or not, because both pages redirect either way — a test that only asserts the destination is green whether the hole is open or shut.

### #6 — Anon-EXECUTE SECURITY DEFINER leaderboard RPCs dump named stats  ✅ CLOSED 2026-07-22 · one accepted residual (was 🟠 HIGH privacy)
**File:** `src/app/actions/leaderboard.ts` (:142, :224, :235, :238)

Verified live grants (`pg_proc.proacl`): all three are `SECURITY DEFINER` and hold EXECUTE for **`anon`, `authenticated`, and PUBLIC** (`=X/postgres`):
- `get_alltime_snapshot_before(p_cutoff, p_club_id DEFAULT NULL)` — with `p_club_id` NULL the filter vanishes; returns `display_name` + games/wins/losses/points_for/against/diff/win_pct for every player in every club, live-recomputed, at any point-in-time via `p_cutoff`.
- `get_session_leaderboard_public(p_session_id)` — returns any session's full named board (names, W/L, points, VIP) with no membership check.
- `get_player_streaks(p_session_id DEFAULT NULL, p_club_id DEFAULT NULL)` — with `{}` returns `(player_id, win_streak)` for every player across all sessions/clubs.

- **Who / today:** **No login at all.** Anyone with the public anon key (shipped in the JS bundle) can POST to `/rest/v1/rpc/...` and pull all 183 CHILLAX members' names + full stats **today**; becomes a genuine cross-club dump at a second club. This is the same anonymous surface as the already-known `v_alltime_leaderboard_mat` matview, but richer (point differentials, per-session boards, point-in-time slicing) and always fresh.
- **Fix:** `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated` (note: revoking anon/authenticated alone is insufficient — PUBLIC holds the grant) and call these via the service role from the server actions, which already resolve the club/session server-side. The public share page (`leaderboard/[sessionId]/page.tsx`) already uses the service-role client, so revoking direct grants does not break it. Alternatively bake `is_club_member(p_club_id)` / `session_access_level(...) IS NOT NULL` into the function bodies and reject NULL scope.

*(**✅ CLOSED 2026-07-22 — historical ranking, as written 2026-07-21.** This cluster, together with the pre-confirmed matview finding, is the second-most-impactful item after #1 — it is fully unauthenticated and live today.)*

> **STATUS 2026-07-22 (PR2 — `fix/tenancy-pr2-lock-leaderboard-reads`): CLOSED, with one accepted residual.**
> Migrations `20260722010000` (additive) + `20260722010001` (revoke) drop `anon`/`authenticated`/PUBLIC EXECUTE on all three RPCs plus `_player_name`, and SELECT on `v_alltime_leaderboard_mat` / `v_match_history` / `v_session_leaderboard`. The reads move to the service client and `getAllTimeLeaderboard` / `getPlayerStats` re-authorize in TypeScript (logged in + active member of the club being read), because a **materialized view can never carry RLS** — the GRANT was the whole control. Session-scoped streaks get a new, narrower `get_session_player_streaks(p_session_id)` gated on `session_access_level(...) IS NOT NULL`, so the browser never needs the unscoped one.
>
> **ACCEPTED RESIDUAL RISK — not closed, deliberately.** `get_session_leaderboard_public` is no longer reachable directly over PostgREST, but the full named board for any **already-known session UUID** is still readable unauthenticated through the `getSessionLeaderboard` server action, which runs as service_role. That is the documented share-link contract (`/leaderboard/[sessionId]`, same class as `/tv` and `/wrapped`): it needs a UUID you were given, and it can no longer be enumerated. What the revoke closes is the *unscoped* dump; what remains is the surface the feature is for. Revisit only if share links are ever meant to expire or require login.
>
> **~~Adjacent hole this PR does NOT cover → PR5.~~ ✅ CLOSED 2026-07-24 — this paragraph is stale, and its "PR5" does not exist.** As written on 2026-07-22 it said: `refresh_alltime_leaderboard()` has `proacl IS NULL` (i.e. PUBLIC EXECUTE) and is SECURITY DEFINER, so anon can still trigger `REFRESH MATERIALIZED VIEW` at will over PostgREST — a free DoS lever, not a read leak. **It was not folded into a "PR5 sweep" (§4 runs 0–7, there is none); it was swept up by #10 instead** — `refresh_alltime_leaderboard` is one of the 16 functions revoked by `20260723000000` (see §2 #10). Re-measured on prod 2026-08-13: `has_function_privilege` is **false for anon and for authenticated**, true for `service_role`. Retained rather than deleted because it is the record of how the DoS lever was found; corrected here because it read as a live exposure with a dangling handoff.

### #7 — Unauthenticated live name feed on `session-events:{id}` broadcast channel  ✅ CLOSED (was 🟡 MEDIUM) · clauses 1–2 shipped and clause 3 declined 2026-07-24; declination recorded here 2026-08-13
**File:** `src/lib/realtime.ts:255` (emit `src/lib/broadcast.ts`)

- **What leaks:** The channel is a plain (non-`private`) broadcast — RLS never applies and `realtime.messages` authorization is never enforced (no `config:{private:true}` anywhere in `src/`). Payloads carry real display names: `organizer_intervention.actorName` + `affectedPlayerIds`, `cap_saturation.anchorPlayerName` + `anchorPlayerId`, plus matchmaking/publish toggle state. Join is keyed only by `sessionId`.
- **Who / today:** Any unauthenticated client that knows a session UUID (from the public `/tv/{id}` etc.) can passively harvest the live organizer-name + player-name + intervention feed for the whole session. Live today within CHILLAX; cross-club later.
- **Fix:** Declare the channel `private` on both emit and subscribe, add a `realtime.messages` policy keyed on `session_access_level(id) IS NOT NULL`, and stop putting display names in payloads — send only ids and let each client resolve names it is authorized to read.

> **STATUS 2026-08-13: CLOSED — clauses 1–2 shipped; clause 3 (strip display names) DECLINED, with evidence.**
> Clauses 1 and 2 shipped in PR #41 (`4bc5cfc`, merged **2026-07-24**): `private: true` on emit and subscribe, plus `session_events_broadcast_read` on `realtime.messages` — migration `20260723100000`, whose *filename* carries its 07-23 authoring date but whose prod stamp is `20260724050234` (verified against `supabase_migrations.schema_migrations`, not inferred). Any "#7 shipped 07-23" phrasing elsewhere is that filename leaking into prose; the merge/apply date is 07-24. As of **2026-08-13** (`04c0062`, PR #63 merged as `c9f2337`) the **refusal** is pinned by Suite RB (`tests/integration/realtime-broadcast-rls.test.ts`, APP_MANIFEST §3.35) rather than only the delivery.
>
> Clause 3 is deliberately **not** implemented — and it was **not** dropped silently: `PENDING_WORK_2026-07-23.md` §2.4 ("Reversed from the original PR4 spec — do NOT strip display names") recorded the reversal, with this same reasoning, **in that very commit** (`4bc5cfc` — verified with `git log -S`). What was never done is amending **this** file, which is why every later reader found a prescribed remedy with no visible disposition. That is what this box fixes. The reasoning, re-derived against the deployed predicates on prod 2026-08-13:
> - Every recipient of one of these messages now satisfies `session_access_level(sid) IS NOT NULL` — `sessions.created_by`, a `session_organizers` row, a club `owner`/`admin`, or an **active** `club_members` row.
> - `profiles_select` is `id = auth.uid() OR can_read_profile(id)`, and `can_read_profile` arm 2 is *"target is queued in a session I can reach"*. `cap_saturation.anchorPlayerName` is always a queued player of **that** session — `src/app/actions/matchmaking.ts:584-593` (`const anchor = pool[0]`) takes it from `pool[0]`, and the pool is `fetchActivePool(supabase, sessionId)` over `v_queue_with_wait_time` (`queue_entries` filtered to that session, `status = 'waiting'`, joined to `profiles` for the name). So every recipient can already `SELECT` that display name. Incremental disclosure: **zero**, and it does not decay after the fact — checkout **UPDATEs** the row to `status='left'` (`src/app/actions/queue.ts:166`, and `remove_player_from_queue_organizer` likewise), and arm 2 carries no status filter. ⚠️ `20260723200000`'s header comment claiming *"`queue_entries` rows are DELETEd on checkout"* is **false** — those are DELETE *policies*, not a code path. No application path deletes; the only **non-test** DELETEs are dev-only `clearSessionData` and the hand-run `supabase/data-fixes/20260608_duplicate_name_data_fix.sql` (which removes the losing duplicate's profile too) — `tests/` deletes freely, as the very test named below does. An earlier draft of this box imported the comment as fact and had to be corrected — and so, it turned out, had four other sites that went uncaught for three weeks (three committed; the memory dir is not a git repo) (`APP_MANIFEST.md`'s `can_read_profile` arms table, `MEMORY.md`'s arms list, the `profiles-select-five-arms` memory, and `tests/integration/rls-edge-cases.test.ts` — whose arm-3 test *title* and setup comment both asserted it while the test manufactured the state with a service-role DELETE and passed), all corrected 2026-08-13 once a review round grepped the whole corpus for the claim rather than re-reading this passage.
> - `organizer_intervention.actorName` and `draft_cap_phase.actorName` name the acting organizer, covered by arm 4 (`session_organizers`), arm 5 (`sessions.created_by`), arm 1 (**shares any active club** with the target — the predicate self-joins `club_members` on `me.club_id = them.club_id`; it is *not* a test against this session's club), **arm 3** (target played a match in a session I can reach — which catches any club owner/admin who has ever played in front of this recipient) **or arm 2** (target is *queued* in one; arm 2 has no status filter, so an organizer who is also in their own session's queue is covered outright). Only one shape escapes all five, and it is best stated over the **recipient**, not the actor: a recipient who can reach the session (so they joined the topic) but shares **no active club at all** with the actor, so arm 1 is unavailable. Holding no membership in *this* session's club is the usual route there, but it is not sufficient on its own — recipient and actor can still overlap in some other club, and arm 1 then covers them. Such a recipient cannot resolve an actor who is a club **owner/admin** with no `session_organizers` row, no reachable match played, no reachable queue entry, **and no session of their own creation that this recipient can reach** — that last conjunct is arm 5, and it was missing from this split until review caught it, because the creator discussion below was spending arm 5 on a different question. Today the recipient class that lands there in practice is the **delegated** (QR-invite) organizer: since #2 was fixed, `createSession` requires an explicit `clubId` and `isClubAdmin`, and `getClubRole` filters `is_active = true` (`src/lib/clubs.ts:161`), so a session **creator** holds an active membership *at creation time* and arm 1 covers them. That is not a permanent property — membership is revocable afterwards (`leaveClub` and `removeMember` both call `club_member_deactivate`, `src/app/actions/clubs.ts:257` / `:333`), so a **post**-fix creator can lose arm 1 too — and when they do, they join the delegated organizer in that same uncovered class. 🪤 **Arm 5 does not rescue them, and the immediately preceding draft of this box claimed it did.** `profiles_select` is `id = auth.uid() OR can_read_profile(id)` (re-read off prod 2026-08-13), so `p_profile_id` binds to *the row being read* — the **actor** named in the payload. Every arm is therefore a predicate on the **actor**, qualified only by what the **recipient** can reach: arm 5 fires when the *actor* created a session the recipient can reach, and is completely silent on whether the *recipient* created anything. Read a directional predicate backwards and it appears to cover a class it cannot touch. The conclusion is unaffected — either way the recipient is a co-organizer of the very session being acted on, and naming the co-organizer who just cleared their board is the entire purpose of the field.
> - So the clause buys no confidentiality and costs the feature. `src/hooks/use-organizer-session.ts:300` renders `payload.actorName ?? "A co-organizer"`; an id-only payload makes every attribution anonymous unless a client-side lookup is added — and that lookup fails in precisely the one uncovered case above, rendering "A co-organizer" anyway.
>
> The clause was written while the topic was **public**, when any anon socket could harvest the feed. Clauses 1–2 removed the population it was protecting against; what is left is a named audience that can already read the names. Re-open only if the join predicate is ever widened beyond `session_access_level(...) IS NOT NULL`.

### #8 — Unfiltered `profiles` realtime subscription  ✅ CLOSED · root cause fixed 2026-07-24 (was 🟡 MEDIUM)
**File:** `src/lib/realtime.ts:324`

- **What leaks:** `subscribeToProfiles()` registers `postgres_changes` on `public.profiles` with no filter; because `profiles_select` is `USING(true)`, every authenticated subscriber gets a live push for every profile INSERT/UPDATE platform-wide (id, display_name, skill_level, vip_tag/theme, needs_rename, flagged_at, timestamps). Fires from `use-session-data.ts` and `use-organizer-queue.ts`.
- **Today vs later:** With one club and `profiles_select USING(true)`, this adds little over what's already readable; its real bite is a **cross-club roster-churn firehose once a second club exists**. Anon does not receive it (authenticated-only policy).
- **Fix (root cause):** Tighten `profiles_select` to shared-club visibility (an `EXISTS` over `club_members` self-join), which fixes both the query-level `USING(true)` exposure and this subscription. Keep the narrow column GRANT as defense-in-depth.

> **✅ STATUS 2026-07-24 — CLOSED.** PR #43 (`ba49fa2`), migration `20260723200000_scope_profiles_select_to_shared_scope.sql`, applied to prod (stamp `20260724050127`). The shipped policy is **not** the shared-club-only predicate sketched above: `profiles_select` is now `id = (select auth.uid()) or can_read_profile(id)`, and `can_read_profile` is a union of **five** arms — shared active club, target queued in a reachable session, target played a match in one, target organizes one, target created one. Club-only would have blanked the board for walk-ins, checked-out players and **delegated** organizers who hold no `club_members` row. Everything above this box describes the pre-fix state; read the migration, not the finding, for current behaviour (#7's status box depends on the five-arm version).

### #9 — `match_players` DELETE metadata leak  ⚖️ ACCEPTED 2026-07-24 — no fix possible (was ⚪ LOW · negligible)
**File:** `src/lib/realtime.ts:195`

- WALRUS skips RLS on DELETE; with no filter, every authenticated subscriber gets a DELETE event for any `match_players` row removed platform-wide. But the table is REPLICA IDENTITY DEFAULT with PK `id` only, so the payload is just an opaque row UUID — no player, match, or team. Leak is timing/volume of teardowns, nothing identifying.
- **Fix (optional):** Drive roster refresh off the already-subscribed `matches` stream and drop `match_players` from the realtime publication, or accept it. Low priority.
- **⚖️ FORMALLY ACCEPTED 2026-07-24 — a decision, not a fix; do not count it among the closed findings.** No RLS policy can fix this — realtime bypasses RLS on DELETE by design — so the only real fix is dropping the table from the publication, which costs a live roster-refresh path for a payload that carries nothing identifying. The fix design above is recorded so a future maintainer does not re-derive it. Re-confirmed still-accepted on 2026-08-10 when the #11 firewall landed: the firewall hides roster *rows*, not DELETE *events*, so members continue to receive opaque draft-clear DELETEs.

### #11 — The draft firewall covers `matches` but not `match_players` → members can read unpublished draft rosters  ✅ CLOSED · applied to production 2026-08-10 (was 🟡 MEDIUM-LOW)
**Files:** policy `match_players_select`; `src/lib/realtime.ts:173` (`subscribeToMatchPlayers`)

> **✅ STATUS 2026-08-10 — APPLIED TO PRODUCTION AND VERIFIED. CLOSED.**
> `supabase/migrations/20260810000001_extend_draft_firewall_to_match_players.sql` was applied to
> prod (`usxftpexoimletqmrggb`) after the code deploy reached READY, and stamped
> `20260810151355 / 20260810000001_extend_draft_firewall_to_match_players`. Its 4-check `DO $$`
> block passed, so the two dependent policies and both role sets are provably unchanged.
> `has_match_access` went `7bf351bbbb050f7571395ff9283b539a` → `846db4a625746a778e9c4c38690bbc6e`;
> its ACL carries the same grantee set as pre-apply (`PUBLIC, postgres, anon, authenticated,
> service_role`), and the re-asserted `GRANT` is additive-only and names none of `PUBLIC` or
> `postgres` — so it widened nothing. Client-side, `use-player-match.ts`
> gained a third `queue_entries` subscription so a drafted player still gets their pull event once
> the roster row is hidden from them.
>
> **Verified functionally, not just structurally.** Inside a rolled-back transaction against prod,
> four synthetic matches were created in the hidden E2E sandbox session and read back through real
> `authenticated`/`anon` roles with real JWT claims:
>
> | actor | pending+unpublished | pending+published | completed | in_progress | orphan `match_id` |
> |---|---|---|---|---|---|
> | plain member | **false** ← the only change | true | true | true | NULL → deny |
> | organizer | true | true | true | true | NULL → deny |
> | anon | false | false | false | false | NULL → deny |
>
> End-to-end `SELECT` on `match_players`: member saw 6 of 8 roster rows (exactly the
> pending+unpublished match withheld), organizer 8, anon 0. The transaction rolled back; prod then
> read 945 matches / 3771 `match_players`, 0 rows in the sandbox session and 0 `deadbeef%`
> leftovers. (The 3771 is corroborated pre-apply by the migration's own blast-radius note,
> 3504 completed + 267 cancelled; the 945 has no separate pre-probe capture, so for that half
> "unchanged" is inferred from the clean sandbox counts rather than diffed.)
> Blast radius held: all 945 existing matches are `completed` or `cancelled`, so **zero** rows lost
> visibility — the change is forward-looking, biting only while an organizer is drafting.
>
> Advisors re-run post-apply (security 44, performance 26): no finding is attributable to this
> change. No advisor baseline was saved beforehand, so this is argued by construction rather than
> by diff — the only DDL was a `CREATE OR REPLACE` body change plus additive grants, with name,
> signature, volatility, security mode and `search_path` all identical, so the only lints that
> *could* move are the SECDEF-executable ones and the permissive-policy count, and both were
> checked. The two `has_match_access` SECURITY DEFINER WARNs (anon + authenticated) are
> pre-existing: the lint keys off effective privilege, `PUBLIC` already held `EXECUTE` before the
> apply, and the migration's `GRANT` names only `anon`/`authenticated`/`service_role` so it cannot
> have created the `=X/postgres` entry. (`20260723000000` had already asserted anon +
> authenticated `EXECUTE` on all six RLS helpers, this one included — see §3 below.) And
> `match_players`/`match_games` still show exactly one permissive SELECT policy each, so no
> `multiple_permissive_policies` lint appeared.
>
> **Still true, and deliberately not fixed here:** realtime skips RLS on DELETE, so members keep
> receiving draft-clear DELETE events. `match_players` is REPLICA IDENTITY DEFAULT, so those
> payloads carry only the PK — no roster data — but the channel does not go silent for members.
> That residue is finding #9, which remains formally accepted.

*Found 2026-08-04 while re-deriving the deferred DB-optimization items; not in the original sweep.*

- **The asymmetry** (verified against prod `pg_policies`, 2026-08-04):

  | Table | Policy | Qual |
  |---|---|---|
  | `matches` | `matches_select_draft_firewall` (RESTRICTIVE) | `organizer → true; member → (status <> 'pending' OR is_published)` |
  | `match_players` | `match_players_select` (PERMISSIVE, only policy) | `has_match_access(match_id)` → `session_access_level(session_id) IS NOT NULL` |

  `has_match_access` asks *"do you have any access to this match's session?"* — a plain member passes. It never asks the draft-firewall question. So the `matches` row of an unpublished draft is hidden from members while **its roster rows are not**.
- **What leaks:** `match_players` carries `match_id`, `player_id` and `team`, so the rows group straight back into the drafted teams, and `player_id` resolves to a display name through `profiles_select`. Any club member gets the **full named roster of a match the organizer has not published yet** — over a plain PostgREST `GET /match_players`, no enumeration needed (the rows hand out the `match_id` the hidden `matches` row would have withheld), and pushed to them live: `subscribeToMatchPlayers` is unfiltered, and `postgres_changes` re-checks this same policy per row, so draft roster INSERTs are delivered to every member of the session's club the instant the organizer generates them.
- **Blast radius / severity:** within-club and within-session — no cross-club or cross-session data crosses, which is why this sits below the tenancy findings above. The harm is that it defeats the *point* of draft mode: the organizer's review-before-publish window is exactly the window in which the roster is readable. A prod count returns **0 rows today** — drafts are transient by nature, so at rest there is nothing to read; the exposure exists only while drafts are pending, i.e. precisely when it matters.
- **Fix:** fold the firewall into `has_match_access` itself rather than bolting a second RESTRICTIVE policy onto `match_players`. The blast radius is exactly two policies — `match_players_select` and `match_games_select` are the **only** two consumers (verified: no RPC body references the helper) — and both want the same behaviour. Folding it in also keeps the cost at one `matches` lookup per row; a separate RESTRICTIVE policy would add a second. Organizers are unaffected (they still short-circuit to `true`); the only newly-denied case is a member reading a `pending`-and-unpublished match, which is the intent.
- **Fixed and applied 2026-08-10.** Shipped exactly as designed above — the CASE was folded into `has_match_access`, both policies were left untouched, and the apply was gated behind the code deploy reaching READY (the client needs its `queue_entries` subscription in place *before* the roster rows go dark, or a drafted player loses their pull event).
  - ⛔ **If this ever needs reverting, use `CREATE OR REPLACE`, never `DROP FUNCTION`.** A plain `DROP` fails `2BP01`, but `DROP … CASCADE` *succeeds* and takes `match_players_select` + `match_games_select` with it — leaving both tables RLS-enabled with no SELECT policy at all. Every browser roster read then returns zero rows **as a success, not an error**, which is the same silent-blackout signature as the 07/25 de-authed-client incident.
  - **The sanctioned revert is `supabase/rollbacks/20260810000001_rollback_has_match_access.sql`** (deliberately outside `migrations/` so `db push` cannot run it). Reproduced here so this document stands alone — **every clause is load-bearing**, and dropping `SECURITY DEFINER` in particular causes a worse blackout than the `DROP CASCADE` above, because the helper's own `SELECT … FROM matches` then hits `matches_select_draft_firewall` and returns NULL for members:

    ```sql
    CREATE OR REPLACE FUNCTION public.has_match_access(p_match_id uuid)
     RETURNS boolean
     LANGUAGE sql
     STABLE SECURITY DEFINER
     SET search_path TO 'public', 'pg_temp'
    AS $function$
      SELECT public.session_access_level(m.session_id) IS NOT NULL
      FROM matches m
      WHERE m.id = p_match_id;
    $function$;
    -- verify: md5(pg_get_functiondef('public.has_match_access(uuid)'::regprocedure))
    --         should return 7bf351bbbb050f7571395ff9283b539a
    ```

    Reverting re-opens this finding. The runbook file also carries the recovery SQL for the case where someone has *already* run `DROP … CASCADE`, including the correct policy definitions (the ones in `20260701000008_club_scoped_rls.sql` carry the pre-consolidation qual and would silently restore the old access rule).

### #11 (older duplicate entry) — The draft firewall covers `matches` but not `match_players`  ⛔ SUPERSEDED (was 🟡 MEDIUM-LOW · EXPLOITABLE TODAY) — see the ✅ CLOSED #11 above
**Files:** policy `match_players_select`; `src/lib/realtime.ts:173` (`subscribeToMatchPlayers`)

> **⛔ SUPERSEDED — this box is STALE and its headline is now FALSE. Do not act on it.**
> This is the *second*, older copy of #11; the authoritative entry is the `✅ CLOSED` one earlier in
> §2, whose status box records the prod apply (stamp `20260810151355`) and the verified truth table.
> The migration below **was** applied later the same day. Kept verbatim rather than deleted because
> it is the record of the pre-apply state and of the hand-apply hazard — but it is history, not a
> handoff. *(Marked 2026-08-13. It is exactly this shape — a stale "STILL OPEN" banner outliving its
> fix — that the closing banner in §1 exists to stop.)*

<details><summary><b>Original 2026-08-04 finding text + 2026-08-10 pre-apply status — kept as history. Every present tense below is now past tense; do not act on it.</b></summary>

**🟠 STATUS 2026-08-10 — FIX WRITTEN, NOT YET APPLIED. STILL OPEN ON PRODUCTION.**
`supabase/migrations/20260810000001_extend_draft_firewall_to_match_players.sql` implements the
fix below (folds the firewall CASE into `has_match_access`, re-asserts grants, and carries a
4-check `DO $$` assertion block that fails the apply loudly if anything drifted). Client-side,
`use-player-match.ts` gained a third `queue_entries` subscription so a drafted player still gets
their pull event once the roster row is hidden from them.
**Migrations in this project are applied by hand — merging the code closes nothing.** Until
someone applies this against prod and runs the post-apply checks at
`20260810000001:208-226`, treat #11 as live. Handoff recorded in `MEMORY.md` →
"UNAPPLIED MIGRATIONS".

*Found 2026-08-04 while re-deriving the deferred DB-optimization items; not in the original sweep.*

- **The asymmetry** (verified against prod `pg_policies`, 2026-08-04):

  | Table | Policy | Qual |
  |---|---|---|
  | `matches` | `matches_select_draft_firewall` (RESTRICTIVE) | `organizer → true; member → (status <> 'pending' OR is_published)` |
  | `match_players` | `match_players_select` (PERMISSIVE, only policy) | `has_match_access(match_id)` → `session_access_level(session_id) IS NOT NULL` |

  `has_match_access` asks *"do you have any access to this match's session?"* — a plain member passes. It never asks the draft-firewall question. So the `matches` row of an unpublished draft is hidden from members while **its roster rows are not**.
- **What leaks:** `match_players` carries `match_id`, `player_id` and `team`, so the rows group straight back into the drafted teams, and `player_id` resolves to a display name through `profiles_select`. Any club member gets the **full named roster of a match the organizer has not published yet** — over a plain PostgREST `GET /match_players`, no enumeration needed (the rows hand out the `match_id` the hidden `matches` row would have withheld), and pushed to them live: `subscribeToMatchPlayers` is unfiltered, and `postgres_changes` re-checks this same policy per row, so draft roster INSERTs are delivered to every member of the session's club the instant the organizer generates them.
- **Blast radius / severity:** within-club and within-session — no cross-club or cross-session data crosses, which is why this sits below the tenancy findings above. The harm is that it defeats the *point* of draft mode: the organizer's review-before-publish window is exactly the window in which the roster is readable. A prod count returns **0 rows today** — drafts are transient by nature, so at rest there is nothing to read; the exposure exists only while drafts are pending, i.e. precisely when it matters.
- **Fix:** fold the firewall into `has_match_access` itself rather than bolting a second RESTRICTIVE policy onto `match_players`. The blast radius is exactly two policies — `match_players_select` and `match_games_select` are the **only** two consumers (verified: no RPC body references the helper) — and both want the same behaviour. Folding it in also keeps the cost at one `matches` lookup per row; a separate RESTRICTIVE policy would add a second. Organizers are unaffected (they still short-circuit to `true`); the only newly-denied case is a member reading a `pending`-and-unpublished match, which is the intent.
- ~~**Not yet fixed.** Filed for triage, not shipped — it touches a hot-path helper on two SELECT policies and wants its own migration + verification pass.~~ **Stale as of 2026-08-10** — shipped and applied that day; see the `✅ CLOSED` #11 above.

</details>

### #10 — 16 mutating `SECURITY DEFINER` RPCs are callable with the **anon** key, and the live-swap RPCs mutate a match they never bound to the authorized session  ✅ CLOSED · applied to production 2026-07-24 (was 🔴 CRITICAL)
**Files:** `src/app/actions/live-match-swap.ts:104→124`, `:206→210`, `:279→284`; RPCs in `supabase/migrations/20260617000000_match_provenance_audit.sql:485`, `:556`, `:626` (plus 13 more across the migration history)

*Found 2026-07-23 during the PR3 review, not in the original sweep. Originally filed as "🟠 HIGH · cross-session write by an organizer"; **the severity was raised after a live probe against prod showed no login is required at all**.*

There are **two independent defects stacked on the same code path**, and the first one removes the "must be an organizer" precondition from the second.

**(a) The RPCs are reachable from the browser with the public anon key.** Every server action in this app is written as `auth → authorize → service-role RPC`, so the RPC grants were never treated as a boundary. But PostgREST exposes every function in `public` to whichever role holds `EXECUTE`, and Supabase's `ALTER DEFAULT PRIVILEGES` stamps `anon` + `authenticated` EXECUTE on each function at `CREATE FUNCTION` time. **Verified against prod by curl with nothing but `NEXT_PUBLIC_SUPABASE_ANON_KEY` and no `Authorization` header:**

```
POST /rest/v1/rpc/swap_player_in_active_match  →  400  {"code":"P0001","message":"MATCH_NOT_ACTIVE"}   ← reached the body
POST /rest/v1/rpc/<control, correctly revoked>  →  401  {"code":"42501","message":"permission denied for function …"}
```

`P0001 MATCH_NOT_ACTIVE` is the function's *own* `RAISE`, i.e. execution got past the privilege check, past the row lock, and into the business logic — it failed only because the match id I supplied was not live. A real (public, from `/tv/[id]`) match id would have gone through. A catalog sweep found the same exposure on **16 volatile SECDEF functions**: `create_match_with_players`, `create_held_cross_court_match`, `swap_player_in_match`, `swap_player_in_active_match`, `swap_match_players`, `swap_teams_in_active_match`, `swap_active_from_ondeck`, `undo_swap_active_from_ondeck`, `revert_match_to_active`, `clear_on_deck_match_atomic`, `clear_all_unpublished_drafts`, `fix_record_swap_player`, `record_match_event`, `compute_session_wrapped`, `refresh_cross_session_stats`, `refresh_alltime_leaderboard`.

Because they are SECURITY DEFINER they run as the owner and **RLS never applies**, so this is not "anon can do what anon may do" — it is anon executing the privileged path. With no account at all someone can create matches, rewrite live rosters, reopen completed matches, wipe every unpublished draft in the club, forge audit events with any actor name, rewrite a player's historical record, and recompute/poison published stats.

**(b) The live-swap RPCs authorize on `sessionId` but mutate by `matchId`** — exactly the #4 defect, but on writes, and with no second id to filter on, so the binding has to be added. Each action gates on `isSessionOrganizer(user.id, sessionId)` and then calls an RPC keyed on a *separately* client-supplied `matchId`. **Verified: none of the four RPCs ever compares `matches.session_id` to `p_session_id`.** They lock and mutate `WHERE id = p_match_id`; `p_session_id` drives only the `queue_entries` reads/writes and the audit stamp, so it silently describes a *different* session from the one being mutated.
  - `swap_player_in_active_match` (:485) — `DELETE FROM match_players`, `INSERT`, `UPDATE matches` on the foreign match.
  - `swap_teams_in_active_match` (:556) — **does not even take `p_session_id`**; it reads `session_id` back *out of the match* purely to stamp the audit event, so the audit trail faithfully recorded the victim's session while the authorization had been performed against the attacker's.
  - `swap_active_from_ondeck` / `undo_swap_active_from_ondeck` (:626) — two client-supplied match ids, both mutated, neither bound.

  The TS layer did not bind them either — `swapPlayerInActiveMatch`'s pre-read was `.eq("match_id", matchId).eq("player_id", outPlayerId)`, with no session predicate.

- **Who / today:** (a) **anyone on the internet holding the anon key**, which ships in the client bundle — no login, no organizer role, no membership. (b) additionally, any organizer of any session (including a self-provisioned one, #2) can rewrite the live roster of a match in someone else's session — pulling a player off court mid-game and substituting one of *their own* waiting players. Cross-session today within CHILLAX; cross-club at a second club. The `MATCH_NOT_ACTIVE` / `PLAYER_UNAVAILABLE` guards constrain *when* a swap works, not *whose* match it is.
- **Why it is worse than #4:** #4 leaked an audit trail; this corrupts live match state and the derived stats, is not undoable by the victim organizer, and — via (a) — needs no account at all.
- **Fix (SHIPPED, two migrations + a TS layer):**
  - `20260723000000_revoke_anon_execute_on_mutating_rpcs.sql` — `grant execute … to service_role` **then** `revoke execute … from public, anon, authenticated` for all 16, with in-migration assertions. The grant must come first: on a from-scratch replay `proacl` is NULL, so the revoke materialises `acldefault` and would otherwise strip `service_role` too. `lookup_active_session` and the six RLS helper functions (`is_club_member`, `session_access_level`, `has_match_access`, `is_session_organizer`, `is_match_club_member`, `is_session_club_member`) deliberately **keep** their anon/authenticated grants — RLS policies invoke them as the calling role — and the migration asserts all six for **both** roles before it commits.
  - `20260723000001_bind_live_swaps_to_session.sql` — `AND session_id = p_session_id` on every match lookup in the four live-swap RPCs; `swap_teams_in_active_match` gains `p_session_id uuid DEFAULT NULL` (appended last as a one-way compatibility shim — it lets an old deploy keep working against the new function, **not** the reverse; see the ordering note below, and note it also leaves that one function invocable *unbound* by a caller who omits the key, which is why the revokes matter for it). Every status guard became `IS DISTINCT FROM`, because with the new predicate a mismatch yields *no row* → a NULL status, and plpgsql treats a NULL `IF` as false — the old `!=` guards would have fallen through.
  - `src/app/actions/live-match-swap.ts` — an `allMatchesInSession()` pre-check on all five call sites, returning `MATCH_NOT_ACTIVE` so it is indistinguishable from "does not exist" (no existence oracle). *(2026-08-10: the call sites are still here; the helper's definition moved to `src/lib/match-session-binding.ts` so it is not itself a dispatchable `"use server"` export.)*
  - Regression coverage: 5 integration tests (LMS-14…18, incl. the mixed own-match/foreign-match pair), 32 unit tests whose refusal cases all assert the RPC is *not called*, and two schema-parity tests — a catalog sweep asserting **no** volatile non-trigger SECDEF function is anon/authenticated-reachable, and a signature pin on the four rewritten RPCs. Validated on a real from-scratch `supabase db reset` replay (236 integration tests green) and by anon curl against the fresh DB returning `42501` on the revoked functions while `lookup_active_session` still answers `200`. 4 SQL + 12 TS mutants, all killed.
  - `undoLiveSwap` also validates every field of the client-round-tripped `ctx` before use. It is the only one of the four entry points that takes ids *and* team letters straight from the caller — the other three read `team` back out of the database or the RPC's OUT params. The ids previously failed closed only by accident (a malformed uuid reached `.eq()` raw and Postgres answered 22P02); `team` did not fail closed at all, since `match_players.team` is `char(1) NOT NULL` with no CHECK constraint.
- **⚠ Deploying the code does NOT close this, and the order is not free.** Migrations in this project are applied by hand; merging the PR ships only the TypeScript half. Both `.sql` files must be run against prod `usxftpexoimletqmrggb`, in this exact sequence:

  ```
  1. apply 20260723000000   (the revokes)
  2. apply 20260723000001   (the session binding)
  3. merge / deploy
  ```

  **2 before 3:** PostgREST resolves an RPC by the set of argument *names* in the body, so migration-first is safe (the old code sends only the keys it knows — 5 and 6 respectively — every required parameter is covered and `p_session_id` defaults) but code-first answers `PGRST202` and breaks every team flip and every `team_swap` undo until the migration lands. **1 before 2:** `20260723000000` acts on `swap_teams_in_active_match`, which `20260723000001` drops and recreates with an extra parameter; an explicit argument list would raise 42883 there, rolling back all 16 revokes in one transaction. That function is addressed by oid, and `20260723000001`'s own assertion 5a aborts if `20260723000000` has not run, so the backwards order rolls back rather than half-applying — but go in the sequence above. Do **not** reason that step 1 is order-free just because nothing in the browser calls the 16: that is true only in isolation and false transitively, since 2 must precede 3 and 1 must precede 2. What "nothing in the browser calls the 16" actually buys is that step 1 can be applied *immediately*, ahead of everything, with zero risk to what is running.

> **✅ STATUS — APPLIED TO PRODUCTION AND RE-VERIFIED. CLOSED.** *(Box added 2026-08-13; the apply happened 2026-07-24. The `⚠ Deploying the code does NOT close this` warning above, and the ordering runbook under it, were written before the apply and are retained as the record of how it was sequenced — they are no longer live warnings.)*
>
> Both migrations are stamped on prod `usxftpexoimletqmrggb`, **in the prescribed order**: `20260724050312 / 20260723000000_revoke_anon_execute_on_mutating_rpcs`, then `20260724050436 / 20260723000001_bind_live_swaps_to_session`. A third stamp, `20260724054744 / reject_null_session_in_swap_teams`, closes the one hole the compatibility shim left open — `p_session_id uuid DEFAULT NULL` meant a caller who simply omitted the key got the *unbound* behaviour back; NULL is now rejected outright rather than merely un-matched.
>
> **(a) re-measured against prod 2026-08-13, not inferred from the migration:** `has_function_privilege('anon', …, 'EXECUTE')` is `false` for every one of the 16, and `true` for `service_role` — so the ordering trap the migration warns about (revoke-before-grant materialising `acldefault` and stripping `service_role`) did not bite. A catalog-wide sweep confirms the invariant the regression test pins: **no volatile, non-trigger SECDEF function in `public` is anon-reachable.** Nine SECDEF functions do still hold anon EXECUTE, all of them correctly: the seven `STABLE` anon-executable functions the migration deliberately preserves — **six** are RLS helpers (`is_club_member`, `session_access_level`, `has_match_access`, `is_session_organizer`, `is_match_club_member`, `is_session_club_member`; five `RETURNS boolean`, and `session_access_level` `RETURNS text`, which is why every call site tests it `IS NOT NULL`), and the seventh is **not a predicate at all**: `lookup_active_session` returns `TABLE(id, name, is_active, club_slug)`, and the migration treats it as its own case — a separate assertion block guarding the logged-out join flow, outside the `FOREACH` loop over the six helpers. 🪤 Do **not** use "backs no policy" as the discriminator: measured against `pg_policy` on prod 2026-08-13, only **four** of the six are referenced by any policy (`is_session_organizer` ×13, `session_access_level` ×6, `has_match_access` ×2, `is_club_member` ×2) — `is_match_club_member` and `is_session_club_member` are referenced by none, exactly like `lookup_active_session`. The return type and the migration's own grouping separate them; current policy usage does not. And `handle_new_user` / `handle_new_session`, which are volatile but `RETURNS trigger` — PostgREST will not dispatch a trigger function, and revoking them would be cosmetic.
>
> **(b) still bound in the TypeScript layer:** `allMatchesInSession` guards all five call sites — `live-match-swap.ts:127`, `:237`, `:321` (two ids), `:494`, `:520` (two ids) — with the helper living in `src/lib/match-session-binding.ts` so it is not itself a dispatchable `"use server"` export.

---

### #12 — Two more "authorize on A, operate on B" write paths that the #4/#10 sweeps missed, plus **eight guard-ORDER instances across seven sites** — two surfaced while fixing (a), one booked by the review gate two days earlier and left unowned, five more found by then sweeping guard order as its own class  ✅ CLOSED — SHIPPED + DEPLOYED 2026-08-13 (was 🟡 MEDIUM ×2 + 🔵 LOW ×6)

> **⚠️ FOUND 2026-08-13, while closing this document — not part of the original 2026-07-21 sweep.** The audit's own closing banner asserted that every finding was dispositioned. That was true of the eleven findings *listed*; it was **not** evidence that the defect *class* was exhausted. Re-running the pattern by hand over all 40 `isSessionOrganizer(` call sites found two live instances that #4 (reads) and #10 (live-swap writes) had both walked past. *(40, not the 41 first written here: `grep -rn "isSessionOrganizer(" src` returns 42 lines, of which one is the declaration at `_shared.ts:75` and one is a comment at `profile.ts:10`. A count inside a paragraph about counting call sites does not get to be approximate.)*

**The shape, restated.** A server action gates with `isSessionOrganizer(user.id, sessionId)` on a **client-supplied** `sessionId`, then operates on a **separately client-supplied** row id that is never checked to belong to that session — while writing through `createServiceClient()`, so RLS is not a second line of defence. Identical to #4 and to #10(b); the two instances below simply were not in either sweep's scope.

**(a) `swapMatchPlayers` — the draft-path sibling of the live swaps** (`src/app/actions/swap-player.ts`).
`20260723000001` bound the four *live*-swap RPCs. The **draft**-path cross-match swap was left unbound, at every layer:

- The RPC `swap_match_players(p_a_match_id, p_a_player_id, p_b_match_id, p_b_player_id, p_actor_id, p_actor_name)` **takes no session argument at all**, so it cannot bind even in principle. It reads `session_id` back out of each match only to stamp `record_match_event`, and never compares the two — the same audit-trail inversion #10 called out for `swap_teams_in_active_match`.
- The TS layer did not bind it either. The existence guard already `SELECT`ed `id, status, session_id` for both matches and **discarded `session_id`**; the organizer guard authorized against the caller-supplied `sessionId`; the player guard matched `match_players` on `.in("player_id", …)` with no session or match filter. **The session ids were being fetched and thrown away, which is why the hole read as covered.**
- Secondary, found during review of the fix: the existence and status guards ran **before** the organizer gate, so any authenticated caller — organizer of nothing — could distinguish "no such match" from "match exists but has started". Pre-existing, unrelated to the cross-session hole, and closed by the same change.
- **Reach:** any authenticated organizer of any session could move a player between one of their own pending matches and a pending match in another session — including another club's, once a second club exists. Since #2 was fixed, self-provisioning an organizer session requires club-admin, so the attacker must already organize *something*; that is the only thing that held this below 🟠.
- 🪤 **What it is *not*:** the RPC is **not** client-reachable. Re-measured on prod 2026-08-13: `has_function_privilege(…,'EXECUTE')` is `false` for both `anon` and `authenticated`, `true` for `service_role` — #10's revokes covered it. So this is an authenticated-organizer hole, not an unauthenticated one, and **the server action is the entire boundary**.

**(b) `callNextMatch` — an unbound `courtId`** (`src/app/actions/matchmaking.ts`).
Takes `(sessionId, courtId)` as two independent client-supplied arguments and gates only on the first. `promoteOnDeckMatchInternal` then stamps `matches.court_id = courtId` and runs `courts.update({status:"in_use"}).eq("id", courtId)` with **no session predicate**, through the service client. An organizer of session A could flip another session's court to `in_use` — taking it out of that board's rotation — and leave their own match pointing at a foreign court. The sibling file `src/app/actions/courts.ts` already guards exactly this shape on its own writes (`:98` appends `.eq("session_id", sessionId)` to the court DELETE); this path simply never got the same treatment.

**(c) `closeSession` — the same guard-ORDER defect, in a third file** (`src/app/actions/sessions.ts:1056-1075` **as found at HEAD** — the fix moves these lines, so read the span off `git show HEAD:` not the working tree). 🔵 LOW. **Not a cross-tenant write** — the organizer gate was present and did block the close. But it ran *below* the session fetch, so an authenticated caller holding a session UUID got three distinguishable replies: `"Session not found."` (does not exist), `"Session is already closed."` **plus `alreadyClosed: true`** (exists, closed), `"Not authorized. Organizer access required."` (exists, active, someone else's). A session-UUID existence-and-status oracle across tenants, in an action whose comment block explicitly reasons about the gate's *presence* and says nothing about its *position*.

Three things make this worth recording rather than filing as a nit:

1. **The repo had already written the rule down and then not applied it here.** `sessions.ts` states it verbatim, in the comment above `applyDraftCapOverride`'s `Promise.all`: *`isSessionOrganizer` returns false for both "not yours" and "does not exist", so a distinct message would turn this into a session-UUID existence oracle.* Its own sibling `renotifySessionClosed` gets the order right. `closeSession` — the older, more consequential of the two — did not.
2. **It is the identical defect this branch was fixing in `swapMatchPlayers`, in a file the sweep never opened.** The enumeration that produced (a) and (b) classified each call site by *which id the write uses*; that question is blind to ordering, so all seven `sessions.ts` sites passed it — correctly, on their own terms. **The shape audit that finds unbound writes does not find unordered guards.** They need separate passes.
3. **It was found by the review gate — on 2026-08-11, two days before it was fixed, and then dropped.** ⚠️ An earlier draft of this line credited "the independent review agent on this branch." Git says otherwise: it was booked as a *minor issue* in `6592864` (PR #55) on 2026-08-11 and carried in `MEMORY.md` for two days with **no owner and no trigger**. `CLAUDE.md`'s review gate allows a "Minor issues" verdict to pass *provided the issues are documented in `MEMORY.md`* — that clause buys a **deferral, not a closure**, and **a booked minor with no owner is indistinguishable from a drop**. The gate worked; the follow-through did not. The `MEMORY.md` entry has been stamped closed in place rather than deleted, because the entry *is* the evidence.

**Fix:** the organizer gate moved above the session fetch — above every lookup keyed on `sessionId`. 🪤 The service client is still built **first**, on purpose (`sessions.ts:1048` gives the reasoning: `isSessionOrganizer` calls `createServiceClient()` itself, so gating ahead of it would throw past the documented `{ success, message }` shape and leave the `try/catch` unreachable), so this is **not** the order `renotifySessionClosed` uses — that one gates at `:1253` and calls `createServiceClient()` at `:1260`. The two share gate-before-**lookup**, not gate-before-**client**; this line claimed both for two rounds and cited the sibling as proof of the half it disproves. The now-unused `created_by` column was dropped from the `SELECT` — 🪤 its comment claimed it was fetched *"for the organizer check below"*, which was false at HEAD: `isSessionOrganizer` does its own lookup and never received it. Post-fix, a non-organizer gets one answer for all three inputs, and `alreadyClosed` is never set for them. **Reach:** an authenticated user could enumerate valid session UUIDs and learn their open/closed state, including in clubs they are not a member of. Guessing a v4 UUID is not feasible, so the practical reach is a UUID learned some other way (a shared link, a leaked log) — which is also why it is LOW, not MEDIUM.

**(d) The same defect at five more sites — found only because (c) was treated as a class rather than as a nit.** 🔵 LOW ×5. Point 2 above says the unbound-write sweep is *structurally* blind to ordering; the obvious consequence is that ordering needs its own sweep, and it had never had one. Running it — enumerate every organizer-gated server action, then ask **not** "is there a gate?" but "what runs *above* it?" — turned up five more, in three files the (a)/(b) sweep had already opened and passed:

| Action | File | Pre-auth reply it leaked |
| --- | --- | --- |
| `endMatchInternal` | `match-lifecycle.ts` | `"Match not found."` **and** `` `Match is already ${status}.` `` — status leaked *above* the gate |
| `updateMatchDetails` | `match-lifecycle.ts` | `"Match not found."` vs `"Not authorized…"` |
| `cancelMatchAction` | `match-lifecycle.ts` | `"Match not found."` vs `"Not authorized…"` |
| `clearOnDeckMatch` | `match-drafts.ts` | `` `Match not found: ${raw PostgREST error text}` `` |
| `swapPlayerInMatch` | `swap-player.ts` | three-way: not-found / already-started / not-authorized |

All five fetch the match row through `createServiceClient()`, so **RLS is no backstop**: the probe answers for any match UUID in any club, to any authenticated caller who organizes nothing. **Fix:** in four of the five, missing-row and not-authorized collapsed into **one condition and one return** — `if (!match || !(await isSessionOrganizer(user.id, match.session_id)))` — every status check moved below the gate, and the PostgREST text `console.error`-ed instead of returned. Expressing it as a single condition is deliberate: two adjacent `if`s can drift back apart under a later edit; one cannot. The model was already in-repo at `live-match-swap.ts:453-460`. `endMatchInternal` is the deliberate exception: `submitMatchScore` reaches it having already proved the caller is a player in this match, so that caller has itself established the row existed and `"Match not found."` tells it nothing new. Its missing-row branch stays separate (`participantVerified ? { message: "Match not found." } : DENIED`) and gets the anti-drift property from a single shared `DENIED` object that every pre-authorization rejection in the function returns. ⚖️ **Accepted cost:** a *transient* fetch failure now also answers `"Not authorized…"` to a legitimate organizer, because any reply that separates "the fetch failed" from "not yours" restores the oracle — the attacker picks when to probe. The PostgREST text is `console.error`-ed server-side, so the cause is not lost; the only safe softening is a retry, never a distinguishable message.

Three things about *how* these were missed are worth keeping:

- 🪤 **`swapPlayerInMatch` broke a rule written down 92 lines above it, in its own file.** `swap-player.ts:62-67` records the indistinguishability rule for its sibling `swapMatchPlayers`; the guard site that violated it is at `:159`, which now holds the fix rather than the defect. **A rule written down in a file is not a rule applied in that file.**
- 🪤 **`updateMatchDetails` carried a careful, correct, and entirely irrelevant ordering argument.** Its header reasons at length about *score-validation vs. gate* ordering — and reads as a clean bill of health for the whole function while the fetch above the gate leaked. **Reasoning carefully about a guard's presence says nothing about its position.** The paragraph now carries a ⚠️ to that effect rather than being deleted; the argument it makes is true.

Changing the wording was only safe because **the message text is not the contract**: `use-swap-state.ts:175/227/280` and `swap-sheet.tsx:220` branch on `result.errorCode === "MATCH_STARTED"`, which is preserved. Every consumer of all four changed strings was grepped first — **no test asserted any of them**, which is itself the finding: five state-revealing replies that nothing pinned.

> **✅ STATUS — CLOSED. Shipped and deployed 2026-08-13: PR [#64](https://github.com/Nayzjuan/badminton-app/pull/64) → `main` `8f4cb78`, Vercel Production reported `success`.** TypeScript-only — no migration to hand-apply, so the merge *is* the deploy. CI green on the PR and again on `main` (Unit 1m16s, Integration 8m41s). 🪤 This box previously read *"NOT YET ON PROD"*; it is stamped rather than deleted because a status line that outlives its state is the exact defect §1's disposition sweep exists to prevent — see the ten headings that carried `EXPLOITABLE TODAY` for weeks after their fixes shipped.
>
> - **(a)** New **guard 3b** in `swapMatchPlayers` rejects unless *both* matches carry `session_id === sessionId`, reusing the rows the existence check already fetched, so it costs no extra round-trip. Guard 4 is narrowed to `.in("match_id", matchIds)`. **The organizer gate was also moved up to run before any match lookup** — the guards that answer questions about specific match ids ("exists?", "pending?", "yours?") must not be reachable by a caller who organizes nothing. That reordering closes a pre-existing pre-auth probe as well, not just the new one. Rejection reports **`MATCH_STARTED` with the not-found wording**, deliberately *not* a distinct cross-session code: it keeps "belongs to another session" indistinguishable from "does not exist", per the precedent `live-match-swap.ts:123-126` records explicitly. The detail is logged server-side instead.
> - **(b)** **Two** court-ownership gates, not one, plus a rowcount check:
>   1. In `callNextMatch` (`matchmaking.ts:183`) — `courts` where `id = courtId AND session_id = sessionId`, reject on no row.
>   2. In `promoteOnDeckMatchInternal` itself (`:822`), at the very top of the function, **before any lookup**. This one is what actually closes (b) for the helper. It exists because `promoteOnDeckMatchInternal` is an `export` of a `"use server"` module: it carries no auth gate of its own, so it is one client-side import away from being dispatchable. *(Measured on the 2026-08-13 build: it is **not** in `.next/server/server-reference-manifest.json`'s `node` map — only `callNextMatch` is — so Next rejects it at `serverModuleMap[actionId]` before `decodeReply`. Reachability is a build-time property of who imports it, not of the `export` keyword, which is precisely why the gate is not conditional on today's manifest.)*
>   3. `.eq("session_id", sessionId)` on the `courts` UPDATE further down, with its rowcount now checked and a 0-row result logged.
>
>   🪤 **Scope that third item precisely — an earlier draft of the fix got this wrong, and the wrong version is the more natural thing to write.** The UPDATE predicate **cannot** be what stops the cross-session write, because the CAS at `:937` has already committed the match row `in_progress` with `court_id = courtId` by the time it runs. Without gate 2, a foreign court would still be stamped onto the match and the caller would still receive `success: true`; only the `courts.status` flip would have been blocked. **A guard placed after the write it defends is not a guard.** What the predicate still buys is the *other* half: it is redundant for all three of today's callers (`callNextMatch` validates first; `endMatch`/`cancelMatch` pass `match.court_id` and `match.session_id` off the same row) yet **not provably vacuous**, because `matches.court_id` is a single-column FK (`initial_schema:223`) — nothing in the schema keeps a match's court inside the match's session, and a row written through this very hole would be exactly that shape. So a 0-row result is reported rather than swallowed, instead of silently showing a live match over an `available` court.
> - **Why no migration.** Adding `v_session_a IS DISTINCT FROM v_session_b` to the RPC body would be **strictly weaker** than guard 3b — it would not catch an attacker passing two matches that both belong to the victim's session. Adding a `p_session_id` parameter would require `DROP` + re-`GRANT` (see #10's ordering trap: `revoke`/`DROP` strips `service_role`) and a hand-application on prod, which is real drift risk for no gain **given that the RPC is service_role-only**. Recorded as a deliberate decision, not an oversight: if that grant ever changes, this reasoning expires and the RPC must be bound.
> - **(c)/(d)** The organizer gate moved above every lookup in all six actions. In four of the five (d) sites the missing-row branch and the not-authorized branch became **one condition and one return**, so they cannot drift apart later; `endMatchInternal` keeps a separate missing-row branch on purpose (a caller `submitMatchScore` already verified as a player has itself proved the row existed) and gets the same property from one shared `DENIED` object. 🪤 Two of the four — `cancelMatchAction` and `clearOnDeckMatch` — were nevertheless first written as two adjacent `if`s returning the same literal, and the branch's review gate, not the author, caught it. Stating the rule in the write-up is not applying it in the diff.
> - **Evidence, by execution rather than inspection.** **Ten** regression pins were added — two *binding* (`M-14`, `Test 8b`) and **eight** *ordering* (`M-14b`, `M-14c`, `Test 1b`, `PA-1`…`PA-5`):
>   - `M-14` (`tests/integration/manual-and-swap.test.ts`) — a **pending** match from another session.
>   - `M-14b` (same file) — a **non-pending** match from another session. This one pins guard *order*, not guard existence: with 3c (pending) ahead of 3b (session), a foreign started match answered `"A match has already started…"` while a nonexistent id answered `"One or both matches could not be found."` — distinguishable, i.e. an existence oracle reintroduced one guard later. Because `errorCode` is `MATCH_STARTED` in **every** branch a foreign match id can reach, an errorCode-only assertion would be blind to that reorder, so **M-14b asserts on `message`**. Verified by physically swapping the two guards and confirming M-14b — and only M-14b — fails.
>   - `M-14c` (same file) — the **other half of the same reordering**, and the one the first three do not cover. M-14/M-14b are both run *by an organizer*, so they only pin ordering among guards 3/3b/3c. Guard 2 (organizer) was originally below guard 3 (exists), which let a caller who organizes **nothing** learn whether a match id exists — the same oracle, one guard earlier, and reachable by any authenticated user. M-14c asserts that a non-organizer gets a byte-identical reply for a real started match id and for a random UUID.
>   - `Test 8b` (`tests/integration/matchmaking.test.ts`) — the cross-session court seizure, asserting both that the victim court was not seized and that no session-A match points at it.
>   - `Test 1b` (`tests/integration/close-session.test.ts`) — **(c)**. A stranger probes an active session, a genuinely-closed session, and a random UUID, and must receive byte-identical `message`, no `alreadyClosed`, and `success: false` for all three. ⚠️ The file's pre-existing `"rejects non-organizer callers"` test passes with the gate in **either** position, because it only ever probes an existing *active* session — the one input on which both orderings agree. That is how the defect survived a test named for exactly it: **a test that asserts a guard rejects is not a test that the guard rejects *first*.**
>   - `PA-1` … `PA-5` (`tests/integration/preauth-oracles.test.ts`, **new Suite PA**) — **(d)**, one per site. Each probes the three inputs that separate the orderings: a real match in **someone else's** session, a real match in a **state-revealing** status, and a syntactically valid **nonexistent** UUID — and pins all three replies byte-identical. Each also carries a positive control (the true organizer, and for `PA-1` a *player in the match*, still gets the distinguishing reply), so none can pass by flattening every caller's answer into one string. `PA-4` additionally asserts the reply does **not** match `/PGRST|coerce|JSON|row/i`; `PA-5` pins `errorCode` as the second channel and asserts the owner's and the stranger's messages differ.
>
>     🪤 `PA-2` also reads the row back to prove no rejected edit landed, and that assertion was **vacuous in two successive drafts** — first because every caller sent the same scores and the read-back ran after the owner's own successful write, then because the strangers' replacement scores (99-0) exceeded `MAX_BADMINTON_SCORE` and would have been refused by `scoreSchema` even with no gate at all. It now uses three distinct *valid* pairs (21-15 seeded / 25-23 attempted / 30-28 written) and was proven falsifiable by perturbation — weaken the gate, relax only the message assertions, and it reports `expected 25 to be 21`. The out-of-bounds probe survives as a separate assertion pinning that score validation still runs **below** the gate. **A negative assertion means nothing unless the thing it forbids could otherwise have happened.**
>     🪤 **The mocked identity is one module-level global** (`authState.currentUserId`, `helpers/mock-auth.ts`). Batching multi-caller probes through `Promise.all` interleaves the writes and every in-flight probe runs as whichever caller set it last — so the suite would pass **because all four probes were the same caller and trivially agreed**. Suite PA's probes are deliberately sequential; its first draft was not.
>
>   M-14 and Test 8b were **proven to fail with their guard removed** (the cross-session swap and the court seizure each actually succeeded: `expected true to be false`). M-14b was proven by swapping 3b/3c; **M-14c by moving guard 2 back below guard 3 — under which M-14 and M-14b both stayed green and only M-14c failed.** That is the whole reason it exists: a guard-ordering pin written from the organizer's seat cannot see a hole that opens one seat lower. **Test 1b by moving `closeSession`'s organizer gate back below the session fetch — under which the file's other seven tests, including the one named `"rejects non-organizer callers"`, all stayed green and only Test 1b failed.** Test 8b, M-14b, M-14c, Test 1b and all five `PA-*` each assert a positive control, so none can pass by rejecting everything.
>
>   **The five `PA-*` were proven together, from a script.** `perturb.py` restores all five pre-fix orderings by exact-string replacement, asserting each pattern occurs **exactly once** so a silently-missed edit cannot masquerade as a passing guard. The **whole** integration suite was then re-run against the perturbed tree: `Test Files 1 failed | 24 passed`, `Tests 5 failed | 278 passed` — the five failures being exactly `PA-1`…`PA-5`. `PA-1` failed on the **status** leak rather than the missing row, which is the more interesting half. The three source files were then restored from a pre-perturbation copy and re-hashed byte-for-byte (`shasum -a 256 -c`, not "looks right") and the suite re-run green. 🪤 This repo has already been bitten by a review agent leaving unreverted worktree mutations; a perturbation run is self-inflicted mutation of exactly that kind and deserves the same proof of restoration.
>
>   ⚠️ **Every one of these eight ordering pins was proven by a perturbation that left the *pre-existing* tests green** — and for (d) that was measured across the entire suite at once: **278 pre-existing tests in 24 files, all green, with five authorization gates simultaneously reordered.** Presence and position need separate assertions.
>
>   🪤 **Four of the six actions already had a "rejects non-organizer" test, and all four stayed green.** `updateMatchDetails` (`N-3`), `clearOnDeckMatch` (`N-8`), `cancelMatchAction` (`G-3b` + `F-cancel-3`) and `closeSession` (`"rejects non-organizer callers"`) — each blind for the same structural reason: they probe only an existing row in the expected status, the one input on which both orderings agree. *(An earlier draft of this paragraph said "six for six." It is four of six: `endMatchAction`'s only authorization test is **unauthenticated** — `score-submission.test.ts:220`, since the `submitMatchScore` "not a player" test returns at `match-lifecycle.ts:83` before `endMatchInternal` runs — and `swapPlayerInMatch` had **no** authorization test at all, `M-8`/`M-9`/`M-10` being organizer-driven state guards. That is worse than the tidy claim, not better: two of the five leaks sat under actions with no authorization coverage there to be blind. **A count asserted inside a lesson about unverified counts is not exempt from being counted.**)*
>
>   🪤 **Round 4 of the review gate found two more rotted cites in this document's own evidence, and fixing them rotted a third set inside the same round.** (i) the `submitMatchScore` "not a player" cite pointed at the `if` (`match-lifecycle.ts:82`), not the `return` (`:83`) — the rule it broke is written out at `swap-player.ts:84`; (ii) the `joinAsCoOrganizer` near-miss was anchored at `sessions.ts:418`, the `session_organizers` **INSERT**-failure branch, which is neither a row fetch nor pre-authorization and so could not be a near-miss for this sweep — the line that actually has the sweep's shape is the passcode lookup at `:380-385` returning `INVALID` at `:388`, deliberately kept because the caller there is *supposed* to be a stranger. The elimination stands; its evidence did not. **An anchor is a claim, checked separately from the sentence it anchors.** (iii) Correcting `swap-player.ts`'s file header — it read "three pre-write guards" over a list of four and never mentioned guard 1b, which this branch had added — moved every line below it and re-staled the four return cites a **fourth** time, plus `:145`→`:159`, `54-59`→`62-67`, 86→92 lines, and `M-14b`'s `:406/:434`→`:420/:448`. **Make every edit to the file, THEN read the numbers, THEN write them down — including the edit that fixes a different comment.**
>
>   🪤 **Round 5 — the note warning that line numbers rot carried two rotted numbers of its own.** `swap-player.ts:84` said its cites had been "11 too low" then "10 too low"; the real shifts were **+15** and **+14**, and neither figure had ever been checked against a second source — both were re-typed from memory inside the paragraph that forbids exactly that. Both are now re-derived *and* corroborated against an independent record: `M-14b`'s 🪤 in `manual-and-swap.test.ts`, plus the cite it annotates, tracks a different pair of lines through the same two edits (`:391/:419` → `:406/:434` → `:420/:448`) and yields the same +15 / +14. 🪤 Both records are new on this branch, so that is **two independent derivations agreeing**, not an older source vouching for a newer one — real evidence, but not provenance, and an earlier draft conflated the two. **A delta with no second witness does not belong in a comment at all.** Same round: "every errorCode-less FAILURE return" read file-wide when it holds only for `swapMatchPlayers` — now scoped, with `swapPlayerInMatch`'s own consumers named (`swap-sheet.tsx:234`, and `handleUndoSwap`'s hardcoded toast at `use-swap-state.ts:282-283`, which drops `message`). **A sentence wider than the fact it rests on is wrong even when nothing it protects is broken.**
>
> **The lesson this finding is really about.** Every prior instance of this class was found by auditing a *symptom* (an events read, an anon grant). (a) and (b) were found by auditing the *shape* — enumerating every call site of the authorization helper and asking, for each, "which id is this gate actually about, and which id does the write use?" **A closed finding list is not a closed defect class**, and this document's banner said the former in words that could be read as the latter. That banner is now scoped explicitly. ⚠️ **And (d) says the same thing one level up: a closed *shape* is not a closed class either.** The shape enumeration asks which id the write uses, which is blind to guard *position* — so all seven `sessions.ts` call sites passed it while `closeSession` leaked. Unbound writes and unordered guards are two sweeps, and the second one only happened because (c) was treated as a class instead of filed as a nit.

---

## 3. WHAT IS SOLID (do not worry about these)

- **RLS itself.** Enabled on all 20 tables; the four helper functions (`is_club_member`, `session_access_level`, `has_match_access`, `is_session_organizer`) are STABLE SECURITY DEFINER with pinned `search_path` and correct club scoping. The deny-all tables (`clubs`, `club_invites`, `club_milestones`, `leaderboard_refresh_state`, `player_renames`) are sound. The SELECT policies on `sessions`/`matches`/`match_players`/`queue`/`courts`/`club_members`/`session_wrapped_stats` are correct.
- **Column GRANTs** correctly withhold `profiles.pin` and `sessions.organizer_passcode` from anon/authenticated (the service role in #1 is what ignores them — that's an app-layer bug, not a GRANT bug).
- **`get_monthly_leaderboard`** is invoker-rights (confirmed `prosecdef=false`), so RLS correctly returns `[]` to anon — the "read any club's monthly board by slug" claim was correctly **refuted**.
- **Security-invoker views** (`v_queue_full_with_wait_time`, `v_queue_with_wait_time`, `v_recent_pairings`) respect RLS.
- **`v_match_history` / `v_session_leaderboard` had anon/authenticated SELECT revoked on prod at audit time** — but only on prod. The revoke came from `20260702152731 revoke_leaderboard_history_view_grants_post_cutover`, a migration applied by hand with **no counterpart in this repo**, so a from-scratch `db reset` still ended with `20260702000007`'s emergency re-grants and a fresh environment was *open* where prod was closed. Sound in production, drift everywhere else. `20260722010001` (PR2) makes the repo match — a no-op on prod, the real fix locally and in CI.
- **These app-layer claims are false positives / correctly refuted — drop them:**
  - *"Leaderboard actions read any club by slug"* — the slug-scoped monthly path returns `[]` for non-members; the real hole is the anon RPC grants in #6, not slug resolution.
  - *"Club command center has no session-organizer gate"* — true that any club member can open it, but that is a within-club authorization nuance, not a tenancy breach; no cross-club data crosses.
  - *"`getH2HRecord` / `joinQueueAction` cross-club H2H"* — no cross-club data is actually returned; weak gate, overstated impact.
  - *"`getWrappedData` unauthenticated"* — accurate mechanics but it is the intentional public Wrapped-share contract (single known player+session link), not a tenancy hole.
  - *"`runEngineForSession` ungated"* — a write/integrity concern, not a data-exposure/tenancy issue.
  - *"`compute_session_wrapped`/`refresh_alltime_leaderboard` anon-executable"* — a guard already blocks the core DoS claim; not a read-tenancy issue. **⚠ Superseded by #10:** the *grant* claim was correct and turned out to be one instance of a 16-function class. Both are in the #10 revoke set.

*(~~One item outside the app layer, already known and confirmed, belongs on the fix list:~~ **✅ CLOSED 2026-07-22 — as written on 2026-07-21 this read:** `v_alltime_leaderboard_mat` is a materialized view — RLS impossible — that is not security_invoker and holds anon+authenticated SELECT, so an unauthenticated curl returns every member's name+club_id+stats. Fix it alongside #6: revoke anon/authenticated SELECT and serve the all-time board through the service role. **That is what happened** — prod stamp `20260722165729`, which is this repo's `20260722010001_lock_leaderboard_reads_to_service_role.sql` (filenames and apply stamps differ throughout this project — migrations are applied by hand; see the note under §2 #7), revoked it together with `v_match_history` and `v_session_leaderboard`, and the board moved to the service client with a TypeScript re-authorization (§2 #6). Re-measured on prod 2026-08-13: `has_table_privilege` for SELECT is **false for anon and for authenticated** on all three views. The paragraph is kept because it records why the matview needed a GRANT-level fix rather than a policy — a matview can never carry RLS.)*

---

## 4. RECOMMENDED ORDER OF WORK

> **✅ 2026-08-13 — this whole ordering is DONE. Items 0–5 and 7 shipped; item 6 is an accepted decision, not a shipped fix.** The ✅ marks below were added at different dates, so read the §1 closing banner for the single current picture. Retained as the record of how it was sequenced and why.

0. **✅ DONE 2026-07-24 (prod stamps `20260724050312` then `20260724050436`, plus `20260724054744`).** Revoke anon EXECUTE on the 16 mutating RPCs and bind the live swaps to their session (#10). *Added 2026-07-23; jumps the queue because it is the only finding that needs no account.* Ships as `20260723000000` + `20260723000001` plus the `allMatchesInSession` guard in `live-match-swap.ts`. **The migrations are hand-applied — merging the PR does not close the hole, and `20260723000001` must be applied _before_ the deploy.**
1. **✅ DONE — all three closed in code.** Close the account-takeover chain (do these together, same PR):
   - `profile.ts` (#1) — bind the target `userId` to the session/club in all four actions.
   - `createSession` (#2) — require and verify club membership; no silent CHILLAX fallback.
   - `joinAsCoOrganizer` (#3) — rate-limit + lockout, higher-entropy passcodes, scope the lookup.
   *(These three are the exploitable-today privilege path; #1 is the payload, #2/#3 the enablers.)*
2. **✅ DONE 2026-07-22 (stamps `20260722165217`, `20260722165729`).** Shut off the unauthenticated data dump: revoke `EXECUTE ... FROM PUBLIC, anon, authenticated` on `get_alltime_snapshot_before`, `get_session_leaderboard_public`, `get_player_streaks`, and revoke anon/authenticated SELECT on `v_alltime_leaderboard_mat`; route those reads through the service role in the actions (#6 + known matview). Live, no-login, today.
3. **✅ DONE 2026-07-23 (PR3).** Fix `getMatchEvents` session-binding (#4) — one-line guard, cheap.
4. **✅ DONE 2026-07-23 (PR3).** Stop granting club membership from link views (#5) — remove `ensureClubMembership` from the two legacy shims; restrict it to the token-validated invite/QR path. Do this **before onboarding a second club** — it is the mechanism that turns every latent cross-club finding into a live one.
5. **✅ DONE 2026-07-24.** Harden realtime before club #2: make `session-events` private + policy'd (#7 — ✅ done 2026-07-24, refusal pinned 2026-08-13; the "strip names from payloads" half was **declined 2026-07-24 and recorded 2026-08-13**, see the status box under §2 #7); tighten `profiles_select` to shared-club visibility (#8 — ✅ done 2026-07-24, PR #43 `ba49fa2` + migration `20260723200000`; also fixed the query-level `USING(true)`).
6. **⚖️ ACCEPTED 2026-07-24 — no action, and not a fix:** the `match_players` DELETE metadata leak (#9). No RLS policy can fix it (realtime bypasses RLS on DELETE by design), and the only real fix costs a live roster-refresh path for a payload that carries nothing identifying. Re-confirmed still-accepted 2026-08-10. See §2 #9.
7. **✅ DONE 2026-08-10 — applied to prod and verified:** extend the draft firewall to `match_players` (#11) — one migration folding the `status <> 'pending' OR is_published` check into `has_match_access`. Applied as stamp `20260810151355`, code deployed first (merge `23ced21`, Vercel READY), in-migration assertions passed, and the member/organizer/anon truth table was confirmed functionally against prod inside a rolled-back transaction. Zero existing rows lost visibility. See the status box under §2 #11.

*(Historical urgency framing, written 2026-07-23 — every "today" in the rest of this paragraph is now **past tense**: 0–5 and 7 are fixed, 6 is an accepted decision. Kept because the reasoning about what gates a second club is still the reason the order was chosen.)* Item 0 is exploitable **today by anyone, with no account**. Items 1–3 and the matview/RPC revokes in item 2 are exploitable **today with the single CHILLAX club**, but need a login. Items 4–5 are the gate that must be closed **before a second club is created**, or they become active cross-tenant breaches on day one of multi-club.

Relevant files: `/Users/miggy-onb/Downloads/badminton-app/src/app/actions/profile.ts`, `/src/app/actions/sessions.ts`, `/src/app/actions/match-events.ts`, `/src/app/actions/leaderboard.ts`, `/src/app/play/[sessionId]/page.tsx`, `/src/app/organizer/[sessionId]/page.tsx`, `/src/lib/clubs.ts`, `/src/lib/realtime.ts`, `/src/app/actions/_shared.ts`, `/src/app/actions/auth.ts`.