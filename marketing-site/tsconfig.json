{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@components/*": ["src/components/*"],
      "@layouts/*": ["src/layouts/*"],
      "@data/*": ["src/data/*"],
      // Points TypeScript at the digital-twin source so `astro check` resolves
      // the SandboxRoot import without requiring file copies in src/sandbox/.
      "@bbmt/digital-twin": ["../digital-twin/src/sandbox/SandboxRoot.tsx"]
    },
    // React 17+ automatic JSX runtime so .tsx files don't need `import React`.
    // Astro's base config sets jsx: "preserve" (TS leaves JSX alone for the
    // bundler) but esbuild needs the explicit runtime hint to emit
    // `jsx-runtime` calls instead of `React.createElement`.
    "jsx": "react-jsx",
    "jsxImportSource": "react"
  }
}
